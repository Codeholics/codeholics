---
import { siFacebook, siGithub, siRss, siX } from 'simple-icons';
import searchIcon from './icons/search.svg?raw';

const siteTitle = Astro.props.title ?? '# Codeholics';
const primaryNavItems = [
  { label: '~/', href: '/', desktopClass: 'text-sm' },
  { label: 'Coding', href: '/tags/coding' },
  { label: 'SysAdmin', href: '/tags/sysadmin' },
  { label: 'InfoSec', href: '/tags/infosec' },
  { label: 'OS', href: '/tags/os' },
  { label: 'Hardware', href: '/tags/hardware' },
  { label: 'Reviews', href: '/tags/review' },
];

const socialLinks = [
  { label: 'X', href: 'https://twitter.com/root_codeholics', icon: siX },
  { label: 'Facebook', href: 'https://www.facebook.com/RootCodeholics', icon: siFacebook },
  { label: 'Github', href: 'https://www.github.com/Codeholics', icon: siGithub },
  { label: 'RSS', href: '/rss.xml', icon: siRss },
];

const desktopNavBaseClass =
  'px-3 py-2 rounded-md font-semibold text-slate-200 hover:text-white hover:bg-white/5 focus:outline-none focus:ring-2 focus:ring-white/10 transition';
---
<header class="w-full bg-gradient-to-b from-slate-900/60 to-transparent backdrop-blur-sm border-b border-white/5 sticky top-0 z-40">
  <div class="max-w-7xl mx-auto px-6 py-3 flex items-center justify-between gap-4">
    <!-- Left: Brand -->
    <div class="flex items-center gap-3">
      <a href="/" class="inline-flex items-center gap-3">
        <span class="text-2xl font-semibold tracking-tight text-white">{siteTitle}</span>
      </a>
    </div>

    <!-- Center: primary nav (desktop) -->
    <nav aria-label="Primary" class="desktop-nav flex-1 justify-center">
      <ul class="inline-flex items-center space-x-4 bg-transparent rounded-md px-2 py-1">
        {primaryNavItems.map((item) => (
          <li>
            <a class={`${desktopNavBaseClass} ${item.desktopClass ?? 'text-lg'}`} href={item.href}>{item.label}</a>
          </li>
        ))}
      </ul>
    </nav>

    <!-- Right: utilities -->
    <div class="flex items-center gap-3">
      <div class="hidden items-center gap-1 md:flex" aria-label="Social links">
        {socialLinks.map((item) => (
          <a
            href={item.href}
            class="inline-flex rounded-md p-2 text-slate-200 hover:bg-white/5 hover:text-white focus:outline-none focus:ring-2 focus:ring-white/10 transition"
            aria-label={item.label}
            title={item.label}
            target="_blank"
            rel="noreferrer noopener"
          >
            <svg class="h-5 w-5 fill-current" viewBox="0 0 24 24" aria-hidden="true">
              <path d={item.icon.path}></path>
            </svg>
          </a>
        ))}
      </div>

      <div class="header-search-group">
        <div class="header-search-anchor">
          <button
            id="search-btn"
            type="button"
            class="header-search items-center justify-center text-slate-200 p-2 rounded-md hover:bg-white/5"
            aria-label="Search"
            aria-controls="desktop-search-form"
            aria-expanded="false"
          >
            <span class="header-search-icon inline-flex h-5 w-5" set:html={searchIcon}></span>
          </button>

          <form
            id="desktop-search-form"
            action="/search.html"
            method="get"
            class="header-search-form hidden"
            role="search"
          >
            <label class="sr-only" for="desktop-search-input">Search</label>
            <input
              id="desktop-search-input"
              name="q"
              class="header-search-input rounded-md border border-white/10 px-3 py-2 bg-white/5 placeholder:text-slate-300 text-white outline-none"
              placeholder="Search"
              aria-label="Search"
              required
            />
            <button type="submit" class="header-search-submit px-3 py-2 rounded-md bg-white/5 text-slate-200 hover:bg-white/10">
              Go
            </button>
          </form>
        </div>
      </div>

      <button id="theme-toggle" class="p-2 rounded-md text-slate-200 hover:bg-white/5" aria-label="Toggle theme" title="Toggle theme" data-theme="auto">
        <!-- sun / moon icon will be updated by JS -->
        <svg id="theme-icon" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden>
          <circle cx="12" cy="12" r="4"></circle>
        </svg>
      </button>

      <!-- Mobile hamburger -->
      <button id="nav-toggle" class="mobile-toggle p-2 rounded-md text-slate-200 hover:bg-white/5" aria-label="Open menu" aria-controls="nav-panel" aria-expanded="false">
        <svg id="hamburger" class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden>
          <path d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile panel -->
  <div id="nav-panel" class="mobile-panel hidden px-4 pb-4 bg-slate-900/80 backdrop-blur-sm">
    <div class="space-y-2">
      {primaryNavItems.map((item) => (
        <a class="block py-2 px-3 rounded hover:bg-white/5 text-slate-200" href={item.href}>{item.label}</a>
      ))}

      <div class="flex items-center gap-2 border-t border-white/10 px-3 pt-3" aria-label="Social links">
        {socialLinks.map((item) => (
          <a
            href={item.href}
            class="inline-flex rounded-md p-2 text-slate-200 hover:bg-white/5 hover:text-white focus:outline-none focus:ring-2 focus:ring-white/10 transition"
            aria-label={item.label}
            title={item.label}
            target="_blank"
            rel="noreferrer noopener"
          >
            <svg class="h-5 w-5 fill-current" viewBox="0 0 24 24" aria-hidden="true">
              <path d={item.icon.path}></path>
            </svg>
          </a>
        ))}
      </div>

      <form action="/search.html" method="get" class="mt-2" role="search">
        <label class="sr-only" for="mobile-search-input">Search</label>
        <div class="flex items-center gap-2">
          <input id="mobile-search-input" name="q" class="w-full rounded-md px-3 py-2 bg-white/5 placeholder:text-slate-300 text-white outline-none" placeholder="Search" aria-label="Search" required />
          <button type="submit" class="px-3 py-2 rounded-md bg-white/5 text-slate-200">Search</button>
        </div>
      </form>
    </div>
  </div>

  <script type="module">
    const THEME_KEY = 'theme';

    const applyTheme = (theme) => {
      const html = document.documentElement;
      if (theme === 'dark') {
        html.classList.add('dark');
      } else if (theme === 'light') {
        html.classList.remove('dark');
      } else if (window.matchMedia?.('(prefers-color-scheme: dark)').matches) {
        html.classList.add('dark');
      } else {
        html.classList.remove('dark');
      }
    };

    const updateThemeIcon = (theme) => {
      const icon = document.getElementById('theme-icon');
      if (!icon) return;
      if (theme === 'dark') {
        icon.innerHTML = '<path d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z" />';
      } else if (theme === 'light') {
        icon.innerHTML = '<circle cx="12" cy="12" r="4"></circle><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41" />';
      } else {
        icon.innerHTML = '<circle cx="12" cy="12" r="4"></circle>';
      }
    };

    const setSearchExpanded = (expanded) => {
      const searchButton = document.getElementById('search-btn');
      const searchForm = document.getElementById('desktop-search-form');
      if (!searchButton || !searchForm) return;

      searchButton.setAttribute('aria-expanded', expanded ? 'true' : 'false');
      searchForm.classList.toggle('hidden', !expanded);
    };

    const closeSearch = (restoreFocus = true) => {
      const searchButton = document.getElementById('search-btn');
      if (!searchButton || searchButton.getAttribute('aria-expanded') !== 'true') return;

      setSearchExpanded(false);
      if (restoreFocus) {
        searchButton.focus();
      }
    };

    const openSearch = () => {
      const searchInput = document.getElementById('desktop-search-input');
      setSearchExpanded(true);
      if (searchInput instanceof HTMLInputElement) {
        searchInput.focus();
        searchInput.select();
      }
    };

    const toggleSearch = () => {
      const searchButton = document.getElementById('search-btn');
      if (!searchButton) return;

      if (searchButton.getAttribute('aria-expanded') === 'true') {
        closeSearch();
      } else {
        openSearch();
      }
    };

    const closeMenu = () => {
      const btn = document.getElementById('nav-toggle');
      const panel = document.getElementById('nav-panel');
      const ham = document.getElementById('hamburger');
      if (!btn || !panel) return;
      btn.setAttribute('aria-expanded', 'false');
      panel.classList.add('hidden');
      panel.classList.remove('open');
      ham?.classList.remove('is-open');
      btn.focus();
    };

    const toggleMenu = () => {
      const btn = document.getElementById('nav-toggle');
      const panel = document.getElementById('nav-panel');
      const ham = document.getElementById('hamburger');
      if (!btn || !panel) return;
      const expanded = btn.getAttribute('aria-expanded') === 'true';
      btn.setAttribute('aria-expanded', expanded ? 'false' : 'true');
      panel.classList.toggle('hidden', expanded);
      panel.classList.toggle('open', !expanded);
      ham?.classList.toggle('is-open', !expanded);
      if (!expanded) {
        panel.querySelector('a, button, input')?.focus();
      } else {
        btn.focus();
      }
    };

    const initNavbar = () => {
      const toggle = document.getElementById('nav-toggle');
      const themeToggle = document.getElementById('theme-toggle');
      const searchButton = document.getElementById('search-btn');
      const searchForm = document.getElementById('desktop-search-form');
      const searchInput = document.getElementById('desktop-search-input');
      if (!toggle || !themeToggle || !searchButton || !searchForm || !searchInput) return;

      const initialTheme = localStorage.getItem(THEME_KEY) || 'auto';
      applyTheme(initialTheme);
      updateThemeIcon(initialTheme);

      toggle.addEventListener('click', toggleMenu);
      searchButton.addEventListener('click', toggleSearch);
      themeToggle.addEventListener('click', () => {
        const current = localStorage.getItem(THEME_KEY) || 'auto';
        const next = current === 'dark' ? 'light' : current === 'light' ? 'auto' : 'dark';
        localStorage.setItem(THEME_KEY, next);
        applyTheme(next);
        updateThemeIcon(next);
      });

      searchInput.addEventListener('keydown', (event) => {
        if (event.key === 'Escape') {
          event.preventDefault();
          closeSearch();
        }
      });

      document.addEventListener('click', (event) => {
        if (searchButton.getAttribute('aria-expanded') !== 'true') return;
        if (!(event.target instanceof Node)) return;
        if (!searchForm.contains(event.target) && !searchButton.contains(event.target)) {
          closeSearch(false);
        }
      });

      window.addEventListener('resize', () => {
        if (window.innerWidth >= 768) {
          const panel = document.getElementById('nav-panel');
          panel?.classList.add('hidden');
          panel?.classList.remove('open');
          toggle.setAttribute('aria-expanded', 'false');
          document.getElementById('hamburger')?.classList.remove('is-open');
        } else {
          closeSearch(false);
        }
      });

      window.addEventListener('keydown', (event) => {
        if (event.key === 'Escape') {
          if (searchButton.getAttribute('aria-expanded') === 'true') {
            closeSearch();
            return;
          }
          if (toggle.getAttribute('aria-expanded') === 'true') {
            closeMenu();
          }
        }
      });
    };

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initNavbar, { once: true });
    } else {
      initNavbar();
    }
  </script>
</header>
